<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Valentine Surprise 💖</title>

<style>
*{box-sizing:border-box}
body{
  margin:0;
  font-family:Poppins,system-ui;
  min-height:100vh;
  overflow:hidden;
  text-align:center;
  color:#fff;
  background:linear-gradient(135deg,#ff758c,#ff7eb3);
}
.page{display:none;min-height:100vh;padding:20px;align-items:center;justify-content:center}
.page.active{display:flex}
.card{
  width:min(92vw,420px);
  background:#ffffff22;
  backdrop-filter:blur(12px);
  border-radius:26px;
  padding:26px;
  box-shadow:0 20px 60px #0003;
}
button{
  padding:14px 26px;
  border:none;
  border-radius:999px;
  font-size:16px;
  cursor:pointer;
  margin:8px;
}
.yes{background:linear-gradient(135deg,#ff6a88,#ff99ac);color:#fff}
.no{background:#333;color:#fff;position:absolute}

input[type=date]{
  width:100%;
  padding:14px;
  border-radius:14px;
  border:none;
  font-size:16px;
}

img{
  max-width:100%;
  border-radius:22px;
  box-shadow:0 18px 40px #0005;
}

/* hearts */
.hearts,.confetti{
  position:fixed;
  inset:0;
  pointer-events:none;
  overflow:hidden;
}
.heart{
  position:absolute;
  font-size:22px;
  animation:fall 6s linear infinite;
}
@keyframes fall{
  from{transform:translateY(-10vh)}
  to{transform:translateY(110vh)}
}

/* confetti */
.confetti span{
  position:absolute;
  width:8px;
  height:14px;
  background:rgba(255,255,255,.9);
  animation:conf 5s linear infinite;
}
@keyframes conf{
  from{transform:translateY(-10vh) rotate(0)}
  to{transform:translateY(110vh) rotate(360deg)}
}

/* final */
.final{
  width:100vw;
  height:100vh;
  background:#000;
  display:flex;
  align-items:center;
  justify-content:center;
}
.final img{
  max-width:90%;
  max-height:80vh;
}
</style>
</head>

<body>

<div class="hearts" id="hearts"></div>
<div class="confetti" id="confetti"></div>

<!-- PAGE 1 -->
<div class="page active" id="page1">
  <div class="card">
    <h1>Will you be my Valentine? 💖</h1>
    <button class="yes" onclick="nextPage(2)">Yes 💘</button>
    <button class="no" id="noBtn">No 😈</button>
  </div>
</div>

<!-- PAGE 2 -->
<div class="page" id="page2">
  <div class="card">
    <h2>Choose date for our 1st date 📅</h2>
    <input type="date" id="datePick">
    <br><br>
    <button class="yes" onclick="checkDate()">Submit 💕</button>
  </div>
</div>

<!-- PAGE 3 -->
<div class="page" id="page3">
  <div class="card">
    <img src="https://images.unsplash.com/photo-1529626455594-4ff0802cfb7e">
    <p style="font-size:18px;margin-top:14px">I'm ready lessgoo 😍</p>
    <button class="yes" onclick="triggerFinal()">LESSGOOO 🚀</button>
  </div>
</div>

<!-- PAGE 4 -->
<div class="page" id="page4">
  <div class="final">
    <img src="https://wallpapers.com/images/hd/funny-profile-picture-1l2l3tmmbobjqd53.jpg">
    <audio id="bgAudio" loop>
      <source src="https://willowy-chocolate-1jlzarih46.edgeone.app/Voicy_Loud Moaning.mp3">
    </audio>
  </div>
</div>

<script>
/* hearts */
const hearts=document.getElementById("hearts");
for(let i=0;i<25;i++){
  const h=document.createElement("div");
  h.className="heart";
  h.textContent="💖";
  h.style.left=Math.random()*100+"%";
  h.style.animationDuration=4+Math.random()*4+"s";
  hearts.appendChild(h);
}

/* confetti */
const conf=document.getElementById("confetti");
for(let i=0;i<40;i++){
  const c=document.createElement("span");
  c.style.left=Math.random()*100+"%";
  c.style.animationDuration=3+Math.random()*3+"s";
  conf.appendChild(c);
}

/* NO button safe movement */
const noBtn=document.getElementById("noBtn");
function moveNo(){
  const rect=noBtn.getBoundingClientRect();
  const pad=10;
  const maxX=window.innerWidth-rect.width-pad;
  const maxY=window.innerHeight-rect.height-pad;
  const x=Math.random()*Math.max(maxX,pad);
  const y=Math.random()*Math.max(maxY,pad);
  noBtn.style.left=x+"px";
  noBtn.style.top=y+"px";
}
noBtn.addEventListener("click",moveNo);
noBtn.addEventListener("touchstart",moveNo);
noBtn.addEventListener("mouseover",moveNo);

/* navigation */
function nextPage(n){
  document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));
  document.getElementById("page"+n).classList.add("active");
}

/* date mandatory */
function checkDate(){
  if(!document.getElementById("datePick").value){
    alert("Date choose karna compulsory hai ❤️");
    return;
  }
  nextPage(3);
}

/* final trigger */
function triggerFinal(){
  nextPage(4);
  const a=document.getElementById("bgAudio");
  a.volume=1.0;
  a.play().catch(()=>{
    const force=()=>{
      a.play();
      document.removeEventListener("click",force);
      document.removeEventListener("touchstart",force);
    };
    document.addEventListener("click",force);
    document.addEventListener("touchstart",force);
  });
}
</script>

</body>
</html>